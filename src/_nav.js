import { routePath } from "./constants/routes"

export const NAVIGATION = {
    Consumer: {
        items: [
            {
                name: "Home",
                url: routePath.HOME
            }
        ]
    }
}